# base/50-expense-api.hpa.yaml
# TASK 2 (RUNNABLE): HPA on the custom Prometheus metric
# expense_inflight_requests, fed via the Prometheus Adapter. Target 10 comes
# from a single-replica saturation test (ramp one pod to the 500ms p99 SLO).
# CPU-only HPA (the prior spec here) is blind to LLM-API-bound latency -- the
# api pod waits on the network, CPU stays low while p99 climbs. Do not also
# set spec.replicas on the Deployment.
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: expense-api
  namespace: expense-dev
  labels:
    app.kubernetes.io/name: expense-api
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: expense-tracking
    app.kubernetes.io/managed-by: kubectl
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: expense-api
  minReplicas: 2
  maxReplicas: 20
  metrics:
    - type: Pods
      pods:
        metric:
          name: expense_inflight_requests
        target:
          type: AverageValue
          averageValue: "10"
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 600   # outlast a bursty dip before scaling down
      policies:
        - type: Percent
          value: 25
          periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 0
      policies:
        - type: Percent
          value: 100
          periodSeconds: 60
      selectPolicy: Max
